{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "go",
        "request": "launch",
        "name": "Launch Go Program",
        "program": "${workspaceFolder}/main.go"
      }
    ]
}